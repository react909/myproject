# Kassir ERP backend
JWT_SECRET=nurcrm-offline-local-secret-change-me
SQLITE_PATH=./nurcrm.db

# Primary store: local PostgreSQL. SQLite above is only the offline fallback cache.
NURCRM_POSTGRES_DSN=postgresql+psycopg://postgres:postgres@127.0.0.1:5432/nurcrm
